# การตั้งค่าหน้าแอดมิน

## ไฟล์ที่เพิ่มใหม่:
1. `admin.html` - หน้าแอดมินสำหรับจัดการออเดอร์
2. `admin-style.css` - CSS สำหรับหน้าแอดมิน
3. `admin-script.js` - JavaScript สำหรับหน้าแอดมิน
4. `google-apps-script-updated.js` - Google Apps Script ที่อัพเดทแล้ว

## ขั้นตอนการตั้งค่า:

### 1. อัพเดท Google Apps Script
1. เปิด Google Apps Script ที่สร้างไว้
2. ลบโค้ดเดิมทั้งหมด
3. คัดลอกโค้ดจากไฟล์ `google-apps-script-updated.js` ไปวาง
4. บันทึกและ **Deploy ใหม่** (สำคัญ!)
5. ตรวจสอบว่า Deploy เป็น "Web app" และ "Who has access: Anyone"

### 2. เปิดใช้งานหน้าแอดมิน
1. เปิดไฟล์ `admin.html` ในเบราว์เซอร์
2. หรือใช้ Live Server ใน VS Code

**หมายเหตุ:** หากยังมีปัญหา CORS ให้ลองรอ 2-3 นาทีหลัง Deploy ใหม่

## ฟีเจอร์หน้าแอดมิน:

### 📊 Dashboard
- แสดงสถิติออเดอร์ทั้งหมด
- จำนวนออเดอร์ที่รอชำระเงิน
- จำนวนออเดอร์ที่ชำระแล้ว

### 🔍 การกรองข้อมูล
- กรองตามสถานะการชำระเงิน
- ค้นหาตามชื่อลูกค้า

### 📝 การจัดการออเดอร์
- ดูรายละเอียดออเดอร์แต่ละรายการ
- อัพเดทสถานะการชำระเงิน (รอชำระเงิน/ชำระแล้ว/ยกเลิก)
- แนบหลักฐานการชำระเงิน
- เพิ่มหมายเหตุ

### 🔄 การซิงค์ข้อมูล
- ข้อมูลจะอัพเดทแบบ real-time กับ Google Sheets
- ปุ่มรีเฟรชสำหรับโหลดข้อมูลใหม่

## การใช้งาน:

1. **ดูรายการออเดอร์**: หน้าแรกจะแสดงรายการออเดอร์ทั้งหมด
2. **อัพเดทสถานะ**: คลิกปุ่ม "📝 อัพเดทสถานะ" ในแต่ละออเดอร์
3. **เปลี่ยนสถานะ**: เลือกสถานะใหม่จาก dropdown
4. **แนบหลักฐาน**: เลือกไฟล์รูปภาพหรือ PDF
5. **เพิ่มหมายเหตุ**: พิมพ์หมายเหตุเพิ่มเติม
6. **บันทึก**: คลิกปุ่ม "💾 บันทึก"

## หมายเหตุ:
- ข้อมูลจะถูกบันทึกลง Google Sheets คอลัมน์ F, G, H
- หลักฐานการชำระเงินจะถูกแปลงเป็น base64 (ในการใช้งานจริงควรอัพโหลดไป Google Drive)
- หน้าแอดมินใช้ URL เดียวกับหน้าลูกค้า