# 🔧 วิธีแก้ปัญหา CORS - Deploy Google Apps Script ใหม่

## ⚠️ ปัญหา
Error: "No 'Access-Control-Allow-Origin' header is present"

## ✅ วิธีแก้ (ทำตามขั้นตอนนี้ทุกขั้นตอน)

### 1. คัดลอกโค้ดใหม่
- เปิดไฟล์ `google-apps-script.js`
- กด Ctrl+A (หรือ Cmd+A บน Mac) เพื่อเลือกทั้งหมด
- กด Ctrl+C (หรือ Cmd+C) เพื่อคัดลอก

### 2. ไปที่ Google Apps Script
- เปิด https://script.google.com
- เลือกโปรเจคของคุณ

### 3. วางโค้ดใหม่
- ลบโค้ดเดิมทั้งหมด
- วางโค้ดใหม่ที่คัดลอกมา
- กด Ctrl+S (หรือ Cmd+S) เพื่อบันทึก

### 4. Deploy ใหม่ (สำคัญมาก!)
**ต้อง Deploy Version ใหม่ ไม่ใช่แค่บันทึก!**

#### วิธีที่ 1: Deploy Version ใหม่
1. คลิก "Deploy" > "Manage deployments"
2. คลิกไอคอน ✏️ (Edit) ที่ deployment ปัจจุบัน
3. ที่ "Version" เปลี่ยนจาก "Version 1" เป็น "New version"
4. พิมพ์ Description: "Add CORS headers"
5. คลิก "Deploy"
6. คัดลอก URL ใหม่

#### วิธีที่ 2: สร้าง Deployment ใหม่
1. คลิก "Deploy" > "New deployment"
2. เลือก Type: "Web app"
3. ตั้งค่า:
   - Description: "Version with CORS"
   - Execute as: "Me"
   - Who has access: "Anyone"
4. คลิก "Deploy"
5. คลิก "Authorize access"
6. เลือกบัญชี Google ของคุณ
7. คลิก "Advanced" > "Go to [Project Name] (unsafe)"
8. คลิก "Allow"
9. คัดลอก URL ที่ได้

### 5. อัพเดท URL ในโค้ด
- เปิดไฟล์ `admin-script.js`
- แก้ไข `GOOGLE_SCRIPT_URL` ให้เป็น URL ใหม่ที่ได้จากขั้นตอนที่ 4
- บันทึกไฟล์

### 6. ทดสอบ
- รีเฟรชหน้า admin.html
- ตรวจสอบว่าโหลดข้อมูลได้แล้ว

## 🔍 ตรวจสอบว่า Deploy สำเร็จ

เปิด URL ที่ได้ในเบราว์เซอร์:
```
https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec?action=getOrders
```

ถ้าเห็น JSON response แสดงว่า Deploy สำเร็จแล้ว

## ❗ หมายเหตุสำคัญ
- **ต้อง Deploy Version ใหม่ทุกครั้งที่แก้โค้ด**
- แค่กด Save ไม่พอ ต้อง Deploy ด้วย
- URL อาจเปลี่ยนถ้าสร้าง Deployment ใหม่
- ต้องรัน Local Server (http://localhost) ไม่สามารถเปิดไฟล์โดยตรง (file://)
